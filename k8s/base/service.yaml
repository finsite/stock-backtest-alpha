---
apiVersion: v1
kind: Service
metadata:
  name: stock-backtest-alpha
spec:
  type: ClusterIP
  selector:
    app: stock-backtest-alpha
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
